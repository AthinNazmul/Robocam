# RoboNeT Camera Tool — Python Dependencies
# Install via: pip install -r requirements.txt
# Note: python3-tk must be installed via apt (not pip)
#   sudo apt install python3-tk

opencv-python>=4.8.0
Pillow>=10.0.0